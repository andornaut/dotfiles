# gtk-application-prefer-dark-theme is deliberately absent: this file is shared by every
# host, and a value here would outrank the per-host theme that ansible-ctrl writes to
# /etc/xdg/gtk-4.0/settings.ini.
[Settings]
